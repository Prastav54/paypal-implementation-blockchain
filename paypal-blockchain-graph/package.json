{
  "name": "paypal-blockchain",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ paypal-blockchain",
    "create-local": "graph create --node http://localhost:8020/ paypal-blockchain",
    "remove-local": "graph remove --node http://localhost:8020/ paypal-blockchain",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 paypal-blockchain",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
